# electron-updater cannot resolve a feed from an unpackaged app, so `pnpm dev`
# reads this file instead. Automatic launch checks stay off in development; use
# 'Check now' under Settings -> Updates to exercise the flow.
provider: github
owner: FlatOinky
repo: FlatOinky
